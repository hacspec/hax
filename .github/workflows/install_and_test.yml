name: Install & test

on:
  pull_request:
  workflow_dispatch:
  push:
    branches: [main, staging, trying]

jobs:
  tests:
    name: nix-action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: cachix/install-nix-action@v20
    - uses: cachix/cachix-action@v12
      with:
        name: hacspec
        authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
    - name: Build
      run: nix build -L

    - name: Install the toolchain
      run: |
        nix profile install nixpkgs#yq
        nix profile install .#rustc
        nix profile install .
        
    - name: Test the toolchain
      run: |
        nix flake check -L

    - name: Checkout specifications
      uses: actions/checkout@v3
      with:
        repository: 'hacspec/specs'
        
    - name: Extract specifications
      run: |
        paths=$(tomlq -r '.workspace.members | .[]' Cargo.toml)
        for cratePath in $paths; do
          crate=$(tomlq -r '.package.name' "$cratePath/Cargo.toml")
          for backend in fstar coq; do
            for skip in $SKIPLIST; do
              if [[ "$skip" == "$crate" || "$skip" == "$crate-$backend" ]]; then
                echo "⛔ $crate [$backend] (skipping)"
                continue 2
              fi
            done
            echo "::group::$crate [$backend]"
            cargo circus -C -p "$crate" \; -i '**' into "$backend"
            echo "::endgroup::"
          done
        done
      env:
        SKIPLIST: |
          hacspec-ristretto-fstar
          hacspec-sha1-fstar
          hacspec-sha256-fstar
          hacspec-hmac-fstar
          hacspec-hkdf-fstar
          hacspec-p256-fstar
          hacspec-bls12-381-fstar
          hacspec-ecdsa-p256-sha256-fstar
          hacspec-aes-fstar
          hacspec-aes128-gcm-fstar
          hacspec-bls12-381-hash-fstar
          hacspec-ed25519-fstar
          hacspec-edwards25519-fstar
          hacspec-edwards25519-hash-fstar
          hacspec-rsa-pkcs1-fstar
          hacspec-bip-340-fstar
          hacspec-rsa-fdh-vrf-fstar
          tls_cryptolib
          hacspec-merlin
          hacspec-edwards25519-ecvrf
          
        
