[package]
name = "cyclic-modules"
version = "0.1.0"
edition = "2021"

[dependencies]
hax-lib = { path = "../../hax-lib" }

[package.metadata.hax-tests]
into."fstar" = { broken = false, snapshot = "stdout", issue_id = "396" }
